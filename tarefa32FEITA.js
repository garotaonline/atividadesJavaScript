// Construir um algoritmo que calcule a média aritmética dos valores de um vetor de inteiros.

soma = 0

let numeros = [5, 8, 10, 9.5, 6.7]

for (var i = 0; i < numeros.length; i++) {
    soma += numeros[i]
}

media = soma / i

console.log(media.toFixed(2))